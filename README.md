# Data-Modeling-Using-Power-BI
Data modeling in Power BI is the process of connecting multiple tables through relationships so they work together as a unified dataset. After cleaning and transforming data in Power Query, the tables are loaded into Power BI’s Model View, where relationships are defined using common keys.
